language: java

notifications:
  email:
    - admin@zac-h.co.uk
  on_failure: always

before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew test build